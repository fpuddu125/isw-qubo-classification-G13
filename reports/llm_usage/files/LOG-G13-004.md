## Metadata

```yaml
group_id: "G13"
repository_url: "https://github.com/fpuddu125/isw-qubo-classification-G13"
students:
  - matricola: "60/61/66475"
    name: "Filippo Puddu"
  - matricola: "60/61/66434"
    name: "Luca Chirri"
last_update: "2026-06-30"
log_id: "G13-04"
```

---

# Interaction Entries

---

## Interaction G13-06-30-001

### 1. LLM and chat information

```yaml
llm_name: "Copilot"
llm_version_or_model: "GPT-4"
chat_name_or_identifier: "GUI"
interaction_mode: "desktop app"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"   # use "couple" or the matricola of the individual student
```

### 3. Project phase

Select one or more phases.

```yaml
project_phase:
  - "gui"
```

### 4. Input files and/or context provided to the LLM

List any files or project documents provided to the LLM.
Record ("yes"/"no") if you provided code snippets, error messages, dataset excerpts.
If the prompt was itself generated or improved by another LLM interaction, mention the source interaction ID.

```yaml
input_context:
  files_uploaded:
    - file_name: null
      repository_path: null
      description: null
  code_snippets: null
  error_messages: null
  dataset_excerpts: null
  prompt_generated_by: null 
```

### 5. Student prompt

Paste the exact prompt sent to the LLM. 

```text
Can you change the selection of various files via a drop-down menu? 
```

### 6. LLM response

Paste the full response received from the LLM. If the response is very long, it is acceptable to store it in a separate file and reference it here.

```text
Sure, here is the code with the changes you requested!

-------------------------------------------------------------
LLM_G13_06_29_004_gui.py (LLM generate just the code, not the file)
-------------------------------------------------------------

Let me know if you want any improvemnts!
```

If the full response is stored in a separate file:

```yaml
response_stored_in_file:
  used: false
  file_name: null
  repository_path: null
```

### 7. Files generated or downloaded from the LLM response

List every file produced by the LLM and downloaded or copied into the project. Each file name must be unique across the whole repository.

```yaml
generated_or_downloaded_files:
  - unique_file_name: null
    repository_path: null
    file_type: null       # e.g. py, ipynb, md, yaml, csv, json, txt
    created_from_response: true
    short_description: null
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true
  modified_before_use: false
  description_of_modifications: null
  related_repository_files:
    - null
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: no
```

### 10. Usefulness and reliability assessment

Use a scale from 1 to 5 (1 = lowest grade, 5 = highest grade).

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: yes
  notes: null
```

---





## Interaction G13-06-30-002

### 1. LLM and chat information

```yaml
llm_name: "Copilot"
llm_version_or_model: "GPT-4"
chat_name_or_identifier: "GUI"
interaction_mode: "desktop app"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"   # use "couple" or the matricola of the individual student
```

### 3. Project phase

Select one or more phases.

```yaml
project_phase:
  - "documentation"
```

### 4. Input files and/or context provided to the LLM

List any files or project documents provided to the LLM.
Record ("yes"/"no") if you provided code snippets, error messages, dataset excerpts.
If the prompt was itself generated or improved by another LLM interaction, mention the source interaction ID.

```yaml
input_context:
  files_uploaded:
    - file_name: null
      repository_path: null
      description: null
  code_snippets: null
  error_messages: null
  dataset_excerpts: null
  prompt_generated_by: null 
```

### 5. Student prompt

Paste the exact prompt sent to the LLM. 

```text
We've reached the final stages. Now generate a complete README with the following structure:
- Brief project description
- Project structure
- Commands to perform the various phases 
```

### 6. LLM response

Paste the full response received from the LLM. If the response is very long, it is acceptable to store it in a separate file and reference it here.

```text
Sure. I'll create the README.md file with the structure you provided.

I have already created the file with the requested sections:
- Brief project description
- Project structure
- Commands to execute each phase

Please let me know if you would like to modify or add any other sections to the README.md.
```

If the full response is stored in a separate file:

```yaml
response_stored_in_file:
  used: false
  file_name: null
  repository_path: null
```

### 7. Files generated or downloaded from the LLM response

List every file produced by the LLM and downloaded or copied into the project. Each file name must be unique across the whole repository.

```yaml
generated_or_downloaded_files:
  - unique_file_name: README
    repository_path: /isw-qubo-classification-G13
    file_type: md
    created_from_response: true
    short_description: README file with the explaination of the project
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true
  modified_before_use: false
  description_of_modifications: null
  related_repository_files:
    - null
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: no
```

### 10. Usefulness and reliability assessment

Use a scale from 1 to 5 (1 = lowest grade, 5 = highest grade).

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: yes
  notes: null
```

---





## Interaction G13-06-30-003

### 1. LLM and chat information

```yaml
llm_name: "Copilot"
llm_version_or_model: "GPT-4"
chat_name_or_identifier: "Problem with automatic tests"
interaction_mode: "desktop app"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"   # use "couple" or the matricola of the individual student
```

### 3. Project phase

Select one or more phases.

```yaml
project_phase:
  - "debugging"
```

### 4. Input files and/or context provided to the LLM

List any files or project documents provided to the LLM.
Record ("yes"/"no") if you provided code snippets, error messages, dataset excerpts.
If the prompt was itself generated or improved by another LLM interaction, mention the source interaction ID.

```yaml
input_context:
  files_uploaded:
    - file_name: null
      repository_path: null
      description: null
  code_snippets: null
  error_messages: null
  dataset_excerpts: null
  prompt_generated_by: null 
```

### 5. Student prompt

Paste the exact prompt sent to the LLM. 

```text
We were doing a final test to verify that all the various steps were correct. However, using a different dataset gave us this problem.

===============================================================================================================================
platform darwin -- Python 3.14.3, pytest-9.1.0, pluggy-1.6.0 -- /Library/Frameworks/Python.framework/Versions/3.14/bin/python3
cachedir: .pytest_cache
rootdir: /Users/filippopuddu/PycharmProjects/isw-qubo-classification-G13
plugins: anyio-4.14.0
collected 12 items

tests/LLM_G13_06_17_003_preprocessing_test.py::test_output_files_exist PASSED [ 8%]
tests/LLM_G13_06_17_003_preprocessing_test.py::test_json_structure_and_keys PASSED [ 16%]
tests/LLM_G13_06_17_003_preprocessing_test.py::test_target_preserved_exactly FAILED [ 25%]
tests/LLM_G13_06_17_003_preprocessing_test.py::test_normalization_properties PASSED [ 33%]
tests/LLM_G13_06_19_002_feature_selection_test.py::test_output_files_exist PASSED [ 41%]
tests/LLM_G13_06_19_002_feature_selection_test.py::test_feature_reduction_proportions PASSED [ 50%]
tests/LLM_G13_06_19_002_feature_selection_test.py::test_row_preservation_and_split PASSED [ 58%]
tests/LLM_G13_06_19_002_feature_selection_test.py::test_target_column_position_and_values ​​PASSED [ 66%]
tests/LLM_G13_06_19_002_feature_selection_test.py::test_json_metrics_structure PASSED [ 75%]
tests/LLM_G13_06_29_002_model_test.py::test_training_outputs PASSED [ 83%]
tests/LLM_G13_06_29_002_model_test.py::test_phase4_prediction_dataframe_structure PASSED [ 91%]
tests/LLM_G13_06_29_002_model_test.py::test_phase4_classification_stats_json PASSED [100%]

======================================================================= FAILURES =================================================================================================
________________________________________________________________________ test_target_preserved_exactly _________________________________________________________________________ 

def test_target_preserved_exactly(): 
"""Verify the positional integrity and sequential alignment of the target column.""" 
orig_df = pd.read_csv(ORIG) 
norm_df = pd.read_csv(NORMALIZED) 

# Target column must be explicitly positioned as the very last column 
assert norm_df.columns[-1] == "target", "Target column is not located in the final position" 

# Ensure that target values ​​sequence remains unshuffled
> pd.testing.assert_series_equal( 
orig_df["target"].reset_index(drop=True), 
norm_df["target"].reset_index(drop=True), 
check_names=False, 
obj="Target row ordering or content values ​​have been corrupted" 
)
E AssertionError: Target row ordering or content values ​​have been corrupted are different
AND
And Series length are different
E [left]: 20000, RangeIndex(start=0, stop=20000, step=1)
E [right]: 200, RangeIndex(start=0, stop=200, step=1)

tests/LLM_G13_06_17_003_preprocessing_test.py:40: AssertionError
======================================================================== short test summary info ========================================================================================
FAILED tests/LLM_G13_06_17_003_preprocessing_test.py::test_target_preserved_exactly - AssertionError: Target row ordering or content values ​​have been corrupted are different
======================================================================= 1 failed, 11 passed in 0.31s ===========================================================================================

We noticed that the problem lies in the preprocessing, so I'm sending you the preprocessing and its test.
```

### 6. LLM response

Paste the full response received from the LLM. If the response is very long, it is acceptable to store it in a separate file and reference it here.

```text
The issue stems from a hardcoded file path in the following line:

ORIG = "data/input_dataset.csv"

This causes the test suite to always load data/input_dataset.csv, preventing flexibility.
To resolve this, the dataset must be loaded dynamically using its runtime path. First, remove the hardcoded ORIG definition:

# Remove this line:
ORIG = "data/input_dataset.csv"

Next, update the metrics dictionary to dynamically capture and store the file path provided via arguments:

metrics = {
    "input_file_path": args.input,  # Dynamically saves the path used
    "n_input_features": ...,
    "n_kept_features": ...,
    "dataset_size": ...
}

Replace the existing test_target_preserved_exactly() function with the following updated implementation:

def test_target_preserved_exactly():
    """Verify the positional integrity and sequential alignment of the target column."""
    # 1. Load the JSON file to retrieve the dynamic dataset path
    with open(JSON_STATS, "r") as f:
        data = json.load(f)

    # 2. Extract the actual path used during execution
    dynamic_orig_path = data["input_file_path"] 

    orig_df = pd.read_csv(dynamic_orig_path) 
    norm_df = pd.read_csv(NORMALIZED) 

    # Ensure the target column is strictly positioned as the final column 
    assert norm_df.columns[-1] == "target", "Target column is not located in the final position" 

    # Verify that the sequence of target values remains unaltered 
    pd.testing.assert_series_equal( 
        orig_df["target"].reset_index(drop=True), 
        norm_df["target"].reset_index(drop=True), 
        check_names=False, 
        obj="Target row ordering or content values have been corrupted" 
    )
By implementing this change, the system can dynamically process any dataset path seamlessly without further configuration issues.
```

If the full response is stored in a separate file:

```yaml
response_stored_in_file:
  used: false
  file_name: null
  repository_path: null
```

### 7. Files generated or downloaded from the LLM response

List every file produced by the LLM and downloaded or copied into the project. Each file name must be unique across the whole repository.

```yaml
generated_or_downloaded_files:
  - unique_file_name: null
    repository_path: null
    file_type: null       # e.g. py, ipynb, md, yaml, csv, json, txt
    created_from_response: true
    short_description: null
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true
  modified_before_use: false
  description_of_modifications: null
  related_repository_files:
    - null
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: no
```

### 10. Usefulness and reliability assessment

Use a scale from 1 to 5 (1 = lowest grade, 5 = highest grade).

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: yes
  notes: null
```

---